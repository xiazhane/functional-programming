N = int(input("Enter the total number of cards (N): "))

# Initialize variable to store the sum of remaining cards
total_sum = 0
for
         while(n*(n+1)//2 <= N)

lost_card = total_sum - remaining_sum

print("The number of the lost card is:", lost_card)
